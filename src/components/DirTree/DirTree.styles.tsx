import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  directoryContainer: {
    backgroundColor: 'lightgrey',
    borderRadius: 10,
    padding: 20
  },
  directoryItem: {
    backgroundColor: 'red'
  }
});
